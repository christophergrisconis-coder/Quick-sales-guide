<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Offline-only mobile banking and credit tracking dashboard for personal use." />
  <title>SECU Style Credit Tracker</title>
  <link rel="preconnect" href="https://api.fontshare.com" />
  <link href="https://api.fontshare.com/v2/css?f[]=general-sans@400,500,600,700&f[]=zodiak@500,700&display=swap" rel="stylesheet">
  <style>
    :root, [data-theme="light"] {
      --text-xs: clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem);
      --text-sm: clamp(0.875rem, 0.8rem + 0.35vw, 1rem);
      --text-base: clamp(1rem, 0.95rem + 0.25vw, 1.125rem);
      --text-lg: clamp(1.125rem, 1rem + 0.75vw, 1.5rem);
      --text-xl: clamp(1.5rem, 1.2rem + 1.25vw, 2.25rem);
      --text-2xl: clamp(2rem, 1.2rem + 2.5vw, 3.5rem);
      --space-1: 0.25rem; --space-2: 0.5rem; --space-3: 0.75rem; --space-4: 1rem; --space-5: 1.25rem; --space-6: 1.5rem; --space-8: 2rem; --space-10: 2.5rem; --space-12: 3rem; --space-16: 4rem;
      --color-bg: #f4f7f9;
      --color-surface: #fbfcfd;
      --color-surface-2: #f1f5f8;
      --color-surface-offset: #e8eef2;
      --color-border: #cfd9df;
      --color-divider: #dde5ea;
      --color-text: #13232d;
      --color-text-muted: #4f6773;
      --color-text-faint: #7f97a2;
      --color-text-inverse: #f8fbfd;
      --color-primary: #005c7a;
      --color-primary-hover: #0b4b62;
      --color-primary-active: #083647;
      --color-primary-highlight: #cfe7ef;
      --color-success: #2f7d4c;
      --color-warning: #a56613;
      --color-error: #b03e56;
      --radius-sm: 0.5rem; --radius-md: 0.85rem; --radius-lg: 1.1rem; --radius-xl: 1.4rem; --radius-full: 9999px;
      --shadow-sm: 0 2px 10px rgba(7, 36, 50, 0.06);
      --shadow-md: 0 14px 36px rgba(7, 36, 50, 0.10);
      --transition-interactive: 180ms cubic-bezier(0.16, 1, 0.3, 1);
      --font-display: 'Zodiak', Georgia, serif;
      --font-body: 'General Sans', system-ui, sans-serif;
    }
    [data-theme="dark"] {
      --color-bg: #10181d;
      --color-surface: #162128;
      --color-surface-2: #1b2a33;
      --color-surface-offset: #22333d;
      --color-border: #314651;
      --color-divider: #253841;
      --color-text: #e6f0f4;
      --color-text-muted: #a6bcc6;
      --color-text-faint: #76909a;
      --color-text-inverse: #102028;
      --color-primary: #5da4bf;
      --color-primary-hover: #78b4cb;
      --color-primary-active: #3f87a0;
      --color-primary-highlight: #1f333b;
      --color-success: #68b284;
      --color-warning: #d59c4f;
      --color-error: #d87087;
      --shadow-sm: 0 2px 10px rgba(0,0,0,.25);
      --shadow-md: 0 14px 36px rgba(0,0,0,.34);
    }
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { -webkit-text-size-adjust: none; text-size-adjust: none; scroll-behavior: smooth; }
    body { min-height: 100dvh; line-height: 1.55; font-family: var(--font-body); font-size: var(--text-base); color: var(--color-text); background: linear-gradient(180deg, var(--color-bg), var(--color-surface-2)); }
    img, svg { display:block; max-width:100%; }
    button, input, select, textarea { font: inherit; color: inherit; }
    button { cursor: pointer; border: none; }
    a { color: inherit; }
    :focus-visible { outline: 2px solid var(--color-primary); outline-offset: 3px; border-radius: var(--radius-sm); }
    .app { width:min(100%, 560px); margin:0 auto; min-height:100dvh; background:color-mix(in srgb, var(--color-surface) 92%, transparent); box-shadow: var(--shadow-md); }
    .header { position:sticky; top:0; z-index:20; background:color-mix(in srgb, var(--color-surface) 92%, transparent); backdrop-filter: blur(12px); border-bottom:1px solid var(--color-divider); padding: var(--space-4); }
    .header-row { display:flex; align-items:center; justify-content:space-between; gap:var(--space-3); }
    .brand { display:flex; align-items:center; gap:var(--space-3); }
    .logo { width:42px; height:42px; border-radius:12px; background: linear-gradient(160deg, var(--color-primary), color-mix(in srgb, var(--color-primary) 60%, white)); display:grid; place-items:center; color:var(--color-text-inverse); box-shadow: var(--shadow-sm); }
    .logo svg { width:24px; height:24px; }
    h1 { font-family: var(--font-display); font-size: var(--text-xl); line-height:1.05; }
    .sub { color: var(--color-text-muted); font-size: var(--text-sm); }
    .icon-btn { min-width:44px; min-height:44px; border-radius: var(--radius-full); background: var(--color-surface-2); border:1px solid var(--color-border); display:grid; place-items:center; transition: all var(--transition-interactive); }
    .icon-btn:hover, .icon-btn:active { background: var(--color-primary-highlight); border-color: var(--color-primary); }
    .content { padding: var(--space-4); display:grid; gap: var(--space-4); }
    .hero-card, .card { background: var(--color-surface); border:1px solid color-mix(in srgb, var(--color-text) 10%, transparent); border-radius: var(--radius-xl); box-shadow: var(--shadow-sm); }
    .hero-card { padding: var(--space-5); display:grid; gap: var(--space-4); }
    .eyebrow { font-size: var(--text-xs); text-transform: uppercase; letter-spacing: .12em; color: var(--color-text-muted); font-weight:700; }
    .hero-copy p { color: var(--color-text-muted); max-width: 38ch; }
    .util-shell { display:grid; grid-template-columns: 132px 1fr; gap: var(--space-4); align-items:center; }
    .ring-wrap { position:relative; width:132px; height:132px; margin-inline:auto; }
    .ring-wrap svg { transform: rotate(-90deg); }
    .ring-center { position:absolute; inset:0; display:grid; place-items:center; text-align:center; }
    .ring-center strong { font-size: var(--text-xl); line-height:1; }
    .ring-center span { font-size: var(--text-xs); color: var(--color-text-muted); text-transform: uppercase; letter-spacing:.08em; }
    .meter-list { display:grid; gap: var(--space-3); }
    .meter-row { display:flex; justify-content:space-between; gap:var(--space-2); font-size: var(--text-sm); }
    .meter-row strong { font-weight:700; }
    .pill { display:inline-flex; align-items:center; gap:.45rem; padding:.55rem .9rem; border-radius: var(--radius-full); background: var(--color-primary-highlight); color: var(--color-primary); font-size: var(--text-sm); font-weight:700; width:max-content; }
    .quick-actions { display:grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-3); }
    .action-btn { min-height: 82px; border-radius: var(--radius-lg); background: var(--color-surface); border:1px solid var(--color-border); padding: var(--space-3); text-align:left; display:grid; align-content:space-between; transition: all var(--transition-interactive); }
    .action-btn:hover, .action-btn:active { transform: translateY(-1px); background: var(--color-primary-highlight); border-color: var(--color-primary); }
    .action-btn span { font-size: var(--text-xs); color: var(--color-text-muted); }
    .section-title { display:flex; justify-content:space-between; align-items:end; gap: var(--space-3); padding: var(--space-4) var(--space-4) 0; }
    .section-title h2 { font-size: var(--text-lg); line-height:1.1; }
    .section-title p { color: var(--color-text-muted); font-size: var(--text-sm); }
    .stats-grid { padding: var(--space-4); display:grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-3); }
    .stat { background: var(--color-surface-2); border-radius: var(--radius-lg); padding: var(--space-4); border:1px solid var(--color-divider); }
    .stat-label { color: var(--color-text-muted); font-size: var(--text-xs); text-transform: uppercase; letter-spacing:.08em; }
    .stat-value { margin-top: .45rem; font-size: var(--text-lg); font-weight:700; }
    .stat-note { margin-top: .35rem; color: var(--color-text-muted); font-size: var(--text-xs); }
    .form-card { padding: var(--space-4); display:grid; gap: var(--space-4); }
    .grid-2 { display:grid; grid-template-columns: 1fr 1fr; gap: var(--space-3); }
    .field { display:grid; gap: .45rem; }
    .field label { font-size: var(--text-xs); font-weight:700; text-transform: uppercase; letter-spacing:.08em; color: var(--color-text-muted); }
    .input, .select, .textarea { width:100%; border-radius: var(--radius-md); border:1px solid var(--color-border); background: var(--color-surface-2); min-height: 48px; padding: .85rem .95rem; }
    .textarea { min-height: 88px; resize: vertical; }
    .segmented { display:flex; gap: var(--space-2); background: var(--color-surface-2); border-radius: var(--radius-full); padding: .25rem; border:1px solid var(--color-border); }
    .segmented button { flex:1; min-height: 42px; border-radius: var(--radius-full); background: transparent; font-weight:700; }
    .segmented .active { background: var(--color-primary); color: var(--color-text-inverse); }
    .submit-row { display:flex; gap: var(--space-3); }
    .btn { min-height: 48px; border-radius: var(--radius-full); padding: 0 1rem; font-weight:700; }
    .btn-primary { flex:1; background: var(--color-primary); color: var(--color-text-inverse); }
    .btn-secondary { background: var(--color-surface-2); border:1px solid var(--color-border); }
    .list-card { padding-bottom: var(--space-2); }
    .filters { display:flex; gap: var(--space-2); padding: var(--space-4); overflow:auto; }
    .chip { border:1px solid var(--color-border); background: var(--color-surface-2); border-radius: var(--radius-full); min-height:40px; padding: 0 .9rem; white-space:nowrap; }
    .chip.active { background: var(--color-primary-highlight); color: var(--color-primary); border-color: var(--color-primary); }
    .transact
